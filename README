в файлике dump.sql лежит дамп таблицы в кот. есть несколько пользователей. в качестве BD, я использовал MySQL.
подпункт с очередями не успел сделать. к серверу делал запросы через консоль "telnet localhost 8000", дальше username@passwd
после список логинов. рассылку делаю при соединении к серверу и разрыве соединения.
сейчас список статусов хранится в фабрике и соответственно при убивании процесса он потеряется. можно попробывать хранить
в NoSQl( к примеру Redis). хорошо подходит под структуру словаря, кот. есть сейчас. ну и если не жалко потерять эти данные.
Каюсь, что пароль передается в открытом виде, хотя в классе авторизации есть интерфейс под hash.
 А так в целом задача похожа на чат.
